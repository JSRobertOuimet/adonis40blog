@layout('main')

@section('content')
  <a href="/posts">Go Back</a>
  <hr>
  <h1>{{post.title}}</h1>
  <p>{{post.body}}</p>
  <hr>

  <div class="d-flex">
    <a href="/posts/edit/{{post.id}}" class="btn btn-dark mr-3">Edit</a>

    <form action="{{ '/posts/' + post.id + '?_method=DELETE' }}" method="POST">
      {{ csrfField() }}
      <button type="submit" class="btn btn-danger">Delete</button>
    </form>
  </div>
@endsection